# System Architecture

## ClipFlow AI - Technical Architecture Document

---

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                 CLIENTS                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Web App    │  │  Mobile App  │  │   API/SDK    │  │  Webhooks    │        │
│  │   (Next.js)  │  │(React Native)│  │  (REST/WS)   │  │  (Outbound)  │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
└─────────┼──────────────────┼──────────────────┼──────────────────┼──────────────┘
          │                  │                  │                  │
          └──────────────────┴─────────┬────────┴──────────────────┘
                                       │
┌──────────────────────────────────────┼──────────────────────────────────────────┐
│                              EDGE LAYER                                          │
│  ┌───────────────────────────────────┴───────────────────────────────────────┐  │
│  │                        Cloudflare (CDN + WAF + DDoS)                       │  │
│  └───────────────────────────────────┬───────────────────────────────────────┘  │
│  ┌───────────────────────────────────┴───────────────────────────────────────┐  │
│  │                    API Gateway (Kong / AWS API Gateway)                    │  │
│  │                    Rate Limiting • Auth • Routing • Logging               │  │
│  └───────────────────────────────────┬───────────────────────────────────────┘  │
└──────────────────────────────────────┼──────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────┼──────────────────────────────────────────┐
│                            APPLICATION LAYER                                     │
│                                      │                                           │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐    │
│  │   Auth     │ │  Content   │ │  Schedule  │ │ Analytics  │ │Integration │    │
│  │  Service   │ │  Service   │ │  Service   │ │  Service   │ │  Service   │    │
│  │  (JWT)     │ │ (CRUD)     │ │ (Cron)     │ │ (Metrics)  │ │ (OAuth)    │    │
│  └────────────┘ └─────┬──────┘ └────────────┘ └────────────┘ └────────────┘    │
│                       │                                                          │
│  ┌────────────────────┴────────────────────────────────────────────────────┐   │
│  │                        MESSAGE QUEUE (Redis/BullMQ)                      │   │
│  └────────────────────┬────────────────────────────────────────────────────┘   │
└───────────────────────┼─────────────────────────────────────────────────────────┘
                        │
┌───────────────────────┼─────────────────────────────────────────────────────────┐
│                  AI PROCESSING LAYER                                             │
│                        │                                                         │
│  ┌─────────────────────┴─────────────────────────────────────────────────────┐  │
│  │                     AI Worker Cluster (GPU Instances)                      │  │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │  │
│  │  │ Transcribe  │ │ Clip Detect │ │ Caption Gen │ │ Video Render│          │  │
│  │  │  (Whisper)  │ │ (Custom ML) │ │ (FFmpeg)    │ │ (FFmpeg)    │          │  │
│  │  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘          │  │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                          │  │
│  │  │ NLP/GPT    │ │ Face Track  │ │ Virality    │                          │  │
│  │  │ (OpenAI)   │ │ (MediaPipe) │ │ Scoring     │                          │  │
│  │  └─────────────┘ └─────────────┘ └─────────────┘                          │  │
│  └───────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘
                        │
┌───────────────────────┼─────────────────────────────────────────────────────────┐
│                    DATA LAYER                                                    │
│                        │                                                         │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│  │ PostgreSQL │ │   Redis    │ │  S3 / R2   │ │ClickHouse  │ │Meilisearch │   │
│  │ (Primary)  │ │  (Cache)   │ │  (Media)   │ │(Analytics) │ │  (Search)  │   │
│  │ + Replicas │ │  + Queue   │ │  + CDN     │ │            │ │            │   │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘ └────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Technology Stack

| Component          | Technology                    | Purpose                           |
| ------------------ | ----------------------------- | --------------------------------- |
| **Frontend**       | Next.js 14 (App Router)       | SSR, SEO, React Server Components |
| **UI**             | TailwindCSS + shadcn/ui       | Styling, components               |
| **State**          | Zustand + React Query         | Client state, server cache        |
| **Backend**        | Node.js (NestJS)              | REST API, business logic          |
| **AI Workers**     | Python (FastAPI)              | ML processing                     |
| **Database**       | PostgreSQL                    | Primary data store                |
| **Cache**          | Redis                         | Session, queue, cache             |
| **Object Storage** | Cloudflare R2 / AWS S3        | Videos, clips, assets             |
| **CDN**            | Cloudflare                    | Global delivery                   |
| **Queue**          | BullMQ (Redis)                | Job processing                    |
| **Search**         | Meilisearch                   | Full-text search                  |
| **Analytics DB**   | ClickHouse                    | Time-series data                  |
| **AI/ML**          | OpenAI Whisper, GPT-4, FFmpeg | Transcription, NLP, video         |
| **Auth**           | NextAuth.js                   | OAuth, JWT                        |
| **Payments**       | Stripe                        | Subscriptions                     |

---

## 🔄 Data Flow

### Video Processing Pipeline

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          VIDEO PROCESSING FLOW                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. UPLOAD                                                                   │
│  ────────                                                                    │
│  User uploads video (or URL) → Validate → Store original in S3             │
│                                           → Create job in queue             │
│                                           → Return job_id to client         │
│                                                                              │
│  2. TRANSCRIPTION                                                            │
│  ───────────────                                                             │
│  Worker picks up job → Download video → Extract audio                       │
│                      → Whisper transcription (word-level timestamps)        │
│                      → Store transcript in PostgreSQL                       │
│                      → Emit 'transcription_complete' event                  │
│                                                                              │
│  3. ANALYSIS                                                                 │
│  ──────────                                                                  │
│  → GPT-4 analyzes transcript for topics, key moments                        │
│  → Computer vision for scene detection, face tracking                       │
│  → Calculate engagement scores per segment                                  │
│  → Store analysis results                                                   │
│                                                                              │
│  4. CLIP DETECTION                                                           │
│  ────────────────                                                            │
│  → ML model identifies best clip boundaries                                 │
│  → Score each potential clip (hook, emotion, coherence)                     │
│  → Rank and select top N clips                                              │
│  → Store clip metadata                                                      │
│                                                                              │
│  5. CLIP GENERATION                                                          │
│  ─────────────────                                                           │
│  For each clip:                                                              │
│    → Extract segment using FFmpeg                                           │
│    → Apply smart reframing (16:9 → 9:16)                                    │
│    → Generate captions with chosen style                                    │
│    → Render multiple aspect ratios (9:16, 16:9, 1:1)                        │
│    → Generate thumbnail                                                     │
│    → Upload to S3/R2                                                        │
│    → Update clip record with URLs                                           │
│                                                                              │
│  6. COMPLETION                                                               │
│  ────────────                                                                │
│  → Mark video as 'completed'                                                │
│  → Send webhook (if configured)                                             │
│  → Send push notification                                                   │
│  → Update usage metrics                                                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📦 Microservices

### Service Breakdown

| Service             | Responsibility                | Stack          |
| ------------------- | ----------------------------- | -------------- |
| `api-gateway`       | Routing, auth, rate limiting  | Kong/Node      |
| `user-service`      | Authentication, profiles      | NestJS         |
| `content-service`   | Videos, clips, assets CRUD    | NestJS         |
| `schedule-service`  | Post scheduling, calendar     | NestJS         |
| `publish-service`   | Social media publishing       | NestJS         |
| `analytics-service` | Metrics collection, reporting | NestJS         |
| `ai-transcribe`     | Speech-to-text                | Python/FastAPI |
| `ai-clip-detect`    | Clip boundary detection       | Python/FastAPI |
| `ai-render`         | Video rendering, captions     | Python/FFmpeg  |
| `webhook-service`   | Outbound webhooks             | NestJS         |

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          SECURITY LAYERS                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Layer 1: EDGE PROTECTION                                                    │
│  ─────────────────────────                                                   │
│  • Cloudflare WAF (OWASP Top 10)                                            │
│  • DDoS protection                                                           │
│  • Bot management                                                            │
│  • TLS 1.3 encryption                                                        │
│                                                                              │
│  Layer 2: API GATEWAY                                                        │
│  ─────────────────────                                                       │
│  • JWT validation                                                            │
│  • API key verification                                                      │
│  • Rate limiting (per user/IP)                                              │
│  • Request validation                                                        │
│                                                                              │
│  Layer 3: APPLICATION                                                        │
│  ─────────────────────                                                       │
│  • RBAC (Role-Based Access Control)                                         │
│  • Resource-level authorization                                              │
│  • Input sanitization                                                        │
│  • CSRF protection                                                           │
│                                                                              │
│  Layer 4: DATA                                                               │
│  ──────────────                                                              │
│  • AES-256 encryption at rest                                               │
│  • Encrypted connections (TLS)                                              │
│  • Signed URLs for media                                                    │
│  • Row-level security (multi-tenant)                                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📈 Scaling Strategy

### Horizontal Scaling

| Component    | Scaling Approach                   |
| ------------ | ---------------------------------- |
| Web Frontend | Vercel edge functions (auto-scale) |
| API Services | Kubernetes pods (HPA based on CPU) |
| AI Workers   | GPU instance auto-scaling group    |
| PostgreSQL   | Read replicas + connection pooling |
| Redis        | Redis Cluster                      |
| S3/R2        | Infinite scale (managed)           |

### Performance Targets

| Metric                 | Target  |
| ---------------------- | ------- |
| API Response (p95)     | < 200ms |
| Video Processing (1hr) | < 5 min |
| Page Load              | < 3 sec |
| Uptime                 | 99.9%   |

---

## 🚀 Deployment

### Infrastructure (AWS/GCP)

```
┌─────────────────────────────────────────────────────────────────┐
│  Production Environment                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Region: us-east-1 (Primary), eu-west-1 (DR)                   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Kubernetes Cluster (EKS/GKE)                            │   │
│  │  ├── api-gateway (3 replicas)                           │   │
│  │  ├── user-service (2 replicas)                          │   │
│  │  ├── content-service (3 replicas)                       │   │
│  │  ├── schedule-service (2 replicas)                      │   │
│  │  └── analytics-service (2 replicas)                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  GPU Auto-Scaling Group                                  │   │
│  │  ├── ai-transcribe (g4dn.xlarge)                        │   │
│  │  ├── ai-clip-detect (g4dn.xlarge)                       │   │
│  │  └── ai-render (g4dn.2xlarge)                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Data Stores                                             │   │
│  │  ├── RDS PostgreSQL (db.r6g.xlarge, Multi-AZ)           │   │
│  │  ├── ElastiCache Redis (cache.r6g.large, Cluster)       │   │
│  │  ├── ClickHouse Cloud                                   │   │
│  │  └── S3 / Cloudflare R2                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### CI/CD Pipeline

```
Code Push → GitHub Actions → Lint/Test → Build Docker → Push to ECR
    → Deploy to Staging → E2E Tests → Manual Approval → Deploy to Prod
```

---

## 📊 Monitoring & Observability

| Tool              | Purpose             |
| ----------------- | ------------------- |
| Datadog / Grafana | Metrics, dashboards |
| Sentry            | Error tracking      |
| Jaeger            | Distributed tracing |
| CloudWatch        | Infrastructure logs |
| PagerDuty         | On-call alerting    |
